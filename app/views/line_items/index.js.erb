// Update contents
$("#line-items tbody").html("<%= j render 'table_page' %>");

// Update headers
$("thead").html("<%= j render 'table_headers' %>");

// Update pagination links to reflect current page
$("div.pagination").replaceWith("<%= j will_paginate @posts %>");
$(ajaxifyPaginate);

$(detectMobile);
$("#ajax_loader").hide();